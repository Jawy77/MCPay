name: shield-verify
version: "0.1.0"
description: "CRE-verified trust layer for autonomous agent x402 payments"
language: typescript
entrypoint: main.ts

configs:
  staging-settings:
    file: config.staging.json
  production-settings:
    file: config.production.json
